# Spring Boot 4 Migration Summary

<!--
  Generated by the `migrate-spring-boot-4` skill. Fill in every section, replace
  every <placeholder>, delete these guidance comments, and keep the section
  order fixed (so summaries look the same across projects). Write the result to
  `spring-boot-4-migration.md` at the project root — NOT under .jeap-migration/,
  which is scratch. Tables carry the overview; use prose only where a "why"
  needs more than a table cell.
-->

> Generated by the `migrate-spring-boot-4` skill on <YYYY-MM-DD>.
> Records what the migration changed and why, so the change can be reviewed
> after the agent session has ended.

## Overview

| | |
|---|---|
| **Spring Boot** | <old> → <new> |
| **Java baseline** | <old> → 25 |
| **jEAP parent POM** | <old> → <new> |
| **Migration command** | `jeap migrate spring-boot-4` |
| **Final build** | ✅ `./mvnw clean verify` green — <N> tests, 0 failures |
| **Agentic fixes** | <N> (see [Agentic changes](#agentic-changes)) |

## Dependency changes

Explicitly-declared parent-POM and dependency changes. Versions managed by the
parent POM moved with it and are not listed individually.

### Version bumps

| Artifact | From | To | Notes |
|---|---|---|---|
| `<groupId:artifactId>` | <old> | <new> | <short note, if any> |
<!-- one row per explicitly-declared bump; the jEAP parent POM is always one of them -->

### Added dependencies

| Artifact | Scope | Why |
|---|---|---|
| `<groupId:artifactId>` | <scope> | <why it had to be added by hand> |
<!-- one row per dependency added by hand during fix-up; omit this subsection if none -->

## Automated changes

The bulk of the diff was applied automatically by `jeap migrate spring-boot-4`
(parent-POM bump, managed-dependency bumps, and the
`UpgradeSpringBoot_4_0_NoOtherMigrations` OpenRewrite recipe — import rewrites,
property renames, Jackson repointing, etc.). These are visible in the git diff
but are **not** enumerated here. Everything under **Agentic changes** is what the
recipe could not do and was applied by the agent.

## Agentic changes

Changes the agent made by hand to drive the migrated project back to a green
build and test run.

| # | Area | Change | Why | Scope |
|---|---|---|---|---|
| 1 | <area> | <what changed> | <why it was needed> | <module / files> |
<!-- one row per distinct fix; collapse trivially-related edits into one row -->

### Notes

<!-- Only for changes whose "why" needs more than a table cell — typically
     behavioral or non-obvious fixes. Reference rows by their # above.
     Delete this section if every row is self-explanatory. -->

**#<n> — <short title>.** <what was wrong, what changed, and why the change is
correct — enough for a reviewer to follow without re-deriving it.>

## Review checklist

Items that warrant especially careful review:

- [ ] <change # / area> — <what specifically to check, e.g. semantics preserved>

**Review the entire diff** — the migration touches build config, application
code, and tests across the reactor. The items above are the ones to scrutinise
most thoroughly; they are not the only changes that need reviewing.

## Verification

- **Baseline (Spring Boot 3):** `./mvnw clean verify` green before migrating — <N> tests passing.
- **Post-migration:** `./mvnw clean verify` over the full reactor — ✅ <N> tests, 0 failures, 0 errors.
- **Host JDK:** 25.
